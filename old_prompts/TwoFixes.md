Fix 1 — Reorder:



"In the app list, move Tidara to be second from the bottom, so the order bottom-up is: Earth Pulse (last), Tidara, then everything else stays as-is."



Fix 2 — Fit all apps on screen:



"All 6 apps must fit on a single mobile screen (viewport height) without scrolling. Reduce the vertical spacing between app entries — tighten the gap between icon, name, and tagline, and reduce the gap between each app card. Also slightly reduce the icon sizes and the header/top section spacing. Test at 667px viewport height (iPhone SE) as the baseline — all 6 apps plus the header should be visible without scrolling. If they still don't fit, make the icons and text slightly smaller. The sacred geometry background mandala can also be scaled down if it's taking too much space."

